name: Mirror main to portfolio-prod

on:
  push:
    branches: [master]

jobs:
  mirror:
    if: github.repository == 'imrd-works/portfolio'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Push to portfolio-prod
        env:
          TOKEN: ${{ secrets.PROD_PUSH_TOKEN }}
        run: |
          git push --force \
            https://x-access-token:${TOKEN}@github.com/imrd-works/portfolio-prod.git \
            HEAD:master
